<UserControl x:Class="P5CCS.App.Views.SketchTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:editor="clr-namespace:P5CCS.Editor;assembly=P5CCS.Editor"
             xmlns:engine="clr-namespace:P5CCS.Engine;assembly=P5CCS.Engine"
             mc:Ignorable="d"
             Loaded="OnLoaded"
             Unloaded="OnUnloaded"
             d:DesignHeight="450" d:DesignWidth="900">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="200" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" MinWidth="300" />
        </Grid.ColumnDefinitions>

        <editor:SketchCodeEditor x:Name="CodeEditor" Grid.Column="0" />

        <GridSplitter Grid.Column="1"
                      Width="4"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      Background="#33808080" />

        <engine:SketchViewport x:Name="Viewport" Grid.Column="2" />
    </Grid>
</UserControl>
